name: CI

on: [pull_request]

jobs:
  cypress-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run Cypress tests 🧪
        uses: cypress-io/github-action@v5
